//! un dato simple es un tipo de dato que cuando se tiene que guardar en memoria se guarda directamente el VALOR que esta tiene

// true
let abrigo = true; // hemos guardado en la memoria una variable llamada abrigo con el valor true

// true
let frio = true;

/* console.log(abrigo === frio); */

// cogiendo el valor de la variable frio y dándoselo a la nueva variable llamada variable
let variable = frio;

variable = false;

console.log(variable);
console.log(frio);



// dirección = 093482039rgn934g23434
let lista = []; // hemos guardado en la memoria un espacio para poder poner el valor de la lista, ese espacio tiene un número de referencia

// dirección = 3029f023902iegg023i
let lista2 = [];

/* console.log(lista == lista2); */