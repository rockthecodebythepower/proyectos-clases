//* el bucle for está divido en tres partes
//* for (declaración de comienzo; límite; iteración)

// iteración es lo que ocurre después de que acabo una vuelta de mi bucle


//! let i = 0; comienzo en 0 con mi variable i
//? i < 10; límite -> voy a seguir ejecutando el código de dentro de mi bucle si el número es menor que 10
//* i++; iterador -> a la variable i le sumo 1 en cada vuelta del bucle

/* for (let i = 10; i >= 0; i = i - 3) {
    console.log(i);
} */

const palabra = "pomelo"; // 6

let frutas = ["pomelo", "kiwi", "manzana", "piña"]; // 4

for (let i = 0; i < frutas.length; i++) {
    
    console.log(frutas[i]);
    
}