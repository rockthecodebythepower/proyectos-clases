let alumn;
// cuando utilizo la palabra reservada let ocurren muchas cosas por detrás
//! le indico a mi motor de procesamiento (google chrome o node.js) -> que me tiene que crear una variable

console.log(alumn);

alumn = "Dimas";

console.log(alumn);
console.log(alumn);
console.log(alumn);
console.log(alumn);

// camel case -> estoEsCamelCase -> cada palabra se identifica con la primera letra en
// mayúscula

// snake case -> esto_es_snake_case -> cada palabra se indetifica con el guión bajo

let alumnThePower; /* ✅ */

let alumn_the_power;

// lista de palabras reservadas
/* 
    break
    case
    catch
    class
    const
    continue
    debugger
    default
    delete
    do
    else
    export
    extends
    false
    finally
    for
    function
    if
    import
    in
    instanceof
    new
    null
    return
    super
    switch
    this
    throw
    true
    try
    typeof
    var
    void
    while
    with
    enum // A partir de ECMAScript 6, reservada para un uso futuro.
    await // A partir de ECMAScript 7, utilizada en contextos asíncronos.
    yield // A partir de ECMAScript 6, utilizada en generadores. 
*/
