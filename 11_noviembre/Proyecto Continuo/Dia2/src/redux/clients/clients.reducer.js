// INITIAL_STATE es un objeto
const INITIAL_STATE = {
    // loading es una propiedad del objeto con el valor inicial en false
}

// clientReducer es una función que recibe dos parámetros
export const clientReducer = (state = INITIAL_STATE, action) => {

    switch (action.type) {
        default:
            return state;
    }

}