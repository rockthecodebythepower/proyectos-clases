// INITIAL_STATE es un objeto
const INITIAL_STATE = {
    // loading es una propiedad del objeto con el valor inicial en false
}

// userReducer es una función que recibe dos parámetros
export const userReducer = (state = INITIAL_STATE, action) => {

    switch (action.type) {
        default:
            return state;
    }

}