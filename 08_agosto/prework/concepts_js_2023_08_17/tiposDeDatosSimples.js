// number -> decimal, negativo, 0 o cualquier otro -> CUALQUIER NÚMERO
let numero = 5;

// string -> siempre van a llevar comillas de cualquier tipo y tienen muchas particularidades
let cadenaDeTexto = "5";

// boolean -> sólo tiene dos valores, (true o false)
let encendido = false;


console.log(numero + 7);

// el operador + -> si tenemos un string concatenará, si tenemos un número sumará
console.log(cadenaDeTexto + 7);