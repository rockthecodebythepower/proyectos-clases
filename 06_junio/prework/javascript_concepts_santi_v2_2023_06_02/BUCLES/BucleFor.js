const array = ["elemento1", "elemento2", "asldkf"];

console.log(array.length);

for (let i = 0; i < array.length; i++) {
    
    //! ESTE BUCLE DA TANTAS VUELTAS COMO ELEMENTOS TENGA DENTRO DE MI ARRAY
    
}