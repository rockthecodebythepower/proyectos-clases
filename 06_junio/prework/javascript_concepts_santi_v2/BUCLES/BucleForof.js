const array = ["elemento1", "elemento2", "asldkf"];

for (let i = 0; i < array.length; i++) {

    //? estoy creando la variable element que será el valor de array[i];
    const element = array[i];

    //! element por cada elemento que tengo en mi array
    
}

//? bucle específico para recorrer un array de la manera normal
for (const elemento of array) {
    
    //! BUCLE QUE DA TANTAS VUELTAS COMO ELEMENTOS TENGO EN MI ARRAY Y A CADA ELEMENTO LE LLAMA COMO LE INDIQUEMOS

}

//! por cada 1000 bucles que se hacen 999 son para recorrer un array y siempre los hacen así la gente
for (let index = 0; index < array.length; index++) {
    const element = array[index];
    
}