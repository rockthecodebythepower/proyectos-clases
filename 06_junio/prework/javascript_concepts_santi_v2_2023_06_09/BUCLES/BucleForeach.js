//! método de array que vino en ES6

const array = ["elemento1", "elemento2", "asldkf"];

array.forEach((elemento) => {
    console.log(elemento);
})

/* const funcion = (elemento) => {
    console.log(elemento);
}

array.forEach(funcion); */

/* const forEachCasero = (array, funcion) => {

    for (let i = 0; i < array.length; i++) {
        
        funcion(array[i]);
        
    }

}

forEachCasero(array, (elemento) => {
    console.log(elemento)
}) */