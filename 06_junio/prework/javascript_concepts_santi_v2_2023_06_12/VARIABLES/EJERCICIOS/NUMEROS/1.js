let numero = Math.random() * 10;

if (Math.round(numero) % 2 === 0) {
    console.log("el número:", Math.round(numero), "es par");
} else {
    console.log("el número:", Math.round(numero), "es impar");
}