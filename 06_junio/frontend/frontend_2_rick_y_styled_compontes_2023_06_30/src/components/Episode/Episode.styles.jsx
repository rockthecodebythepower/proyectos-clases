import { styled } from "styled-components";
import { LocationDimension, LocationName, LocationWrp } from "../Location/Location.styles";

export const EpisodeWrp = styled(LocationWrp) `
    justify-content: space-between;
    height: 450px;
`

export const EpisodeName = styled(LocationName) `

`

export const EpisodeAirDate = styled(LocationDimension) `

`